APP_ENV=local           # keep "local" while developing

DB_HOST=127.0.0.1
DB_PORT=3306            # change to 8889 if your MySQL runs there
DB_NAME=promapautomation
DB_USER=root
DB_PASS=                # blank for MAMP's default root/no-pw
JWT_SECRET=your-jwt-secret

# (SendGrid keys removed because OTP is gone)
